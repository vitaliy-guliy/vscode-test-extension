{
  "name": "vscode-test-get-session",
  "publisher": "test",
  "displayName": "Test Extension",
  "description": "Test Extension",
  "version": "0.0.2",
  "repository": {
    "type": "git",
    "url": "https://github.com/vitaliy-guliy/vscode-test-extension"
  },
  "engines": {
    "vscode": "^1.74.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onStartupFinished"
  ],
  "main": "./out/extension.js",
  "scripts": {
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "build-vsix": "vsce package",
    "cleanup": "rm -f ./vscode-test-get-session-0.0.2.vsix && rm -rf ./out && rm -rf node_modules",
    "build": "npm install && npm run compile && npm run build-vsix"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/node": "16.x",
    "@types/vscode": "^1.74.0",
    "typescript": "^4.9.4",
    "vsce": "^2.15.0"
  },
  "contributes": {
    "commands": [
      {
        "command": "test.test-get-session",
        "title": "Test: Get session"
      }
    ]
  }
}
